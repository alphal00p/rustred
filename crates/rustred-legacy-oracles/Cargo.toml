[package]
name = "rustred-legacy-oracles"
version.workspace = true
edition = "2024"
rust-version = "1.89"
publish = false
description = "Historical authored validation oracles for the RustRed core"
license = "MIT"
readme = "README.md"

[dependencies]
rustred = { path = "../..", default-features = false, features = ["legacy-oracle-support"] }
