# Four-loop development checkpoint — 2026-09-17

For the detailed reader-facing report, see
[STATUS_17_09_2026.md](../../STATUS_17_09_2026.md).

This is a quota checkpoint, not completion of the four-loop objective. The user
requested no new feature development, a stable tested snapshot, a consolidated
measurement/status report, and a stop after publication. Work was frozen toward
an approximately 08:42 UTC deadline.

## What is usable

- Published K1/K3/K6 generation and the through-three-loop Vakint backend remain
  the production baseline. The previously recorded lower-loop selection passed
  83 tests; that historical result must not be mistaken for a fresh checkpoint
  run or four-loop coverage.
- Four-loop H/X/BMW/FG sparse-exact candidate generation completes. Candidates
  are explicitly uncertified; they are not shipped four-loop closing artifacts.
- Saved candidate formulas can now be loaded for experimental application
  without repeating search. The loader preserves physical index coordinates,
  correctly inverts saved coordinate priorities, and checks family binding and
  ingress limits. RustRed owns application; Vakint does not duplicate it.
- Symbolica reconstruction now uses pivot-branch tracking, independently varied
  finite-field coordinates and an exact sparse comparison/support-retry gate.
  It uses Symbolica's reconstruction and algebra, not a new CAS kernel. Exact
  sparse replay is included in its measured solver workload. The separately
  frozen matrix completed all eight rows; all 1,808 output files match sparse
  exact. Its provenance and final outcomes are in the benchmark report.
- Guard proof scheduling and joint affine/domain consequences address concrete
  certification failures. A finite batch can separately replay original-source
  identities; the finite-target application report remains explicitly *not* a
  certificate. No four-loop source-replayed, cold-reloadable closing artifact is
  claimed at this checkpoint.

## Responsible lanes

| Lane | Responsible agent | Checkpoint task |
| --- | --- | --- |
| Reconstruction | `reconstruction_matrix` | Finish feasible one/six-worker matrix, compare every saved output with sparse exact, retain failures/censored observations |
| C++ SpIRed and output comparison | `cpp_output_equivalence` | Uncapped full-parent runs; compare reduction values after resolving differing finite master bases |
| Independent audit and Vakint measurements | `semi_pivot_audit` | Audit implementation/math claims and finite-target FMFT/native timing boundaries |
| Integration, tests and publication | `/root` | Freeze source, verify, document, commit/push subject to repository gates, stop owned work |

No agent is authorized to start a new feature during this checkpoint. Unfinished
objectives remain resumable, not silently declared complete.

## Vakint four-loop status

The opt-in experimental bridge has passed parent, dotted-parent and pinch
comparisons for H, FG, X and BMW: **twelve finite-target comparisons**. The cold
native tail has an invalid FORM path and performs 26,956 / 3,362 / 82,637 / 16,113
new rule applications respectively for the dotted targets. BMW uses first-slot
power three rather than two to exercise an actual recurrence. Offline FMFT prepares exact
projections for the reached finite terminals. This is not a shipped standalone
four-loop master catalog and does not establish 20,000-digit data.

The existing fourteen analytic four-loop FMFT references plus the actually
four-loop decorated freeform reference passed: **15/15 legacy tests**. These
are not fifteen RustRed acceptance passes. BMW's original power-two targets were
all terminals, so that earlier attempt stopped at the test's required
recurrence-use assertion; the subsequent power-three experiment passed without
weakening that assertion. This is not evidence of arbitrary-index coverage.

The shared FMFT finalizer now expands exact Laurent coefficients before
approximate master substitution. A regression demonstrates the old spurious
floating cancellation, and the missing-order rejection and tolerances remain
unchanged. Requested precision still must be interpreted against supplied
master-table precision; no added arbitrary-precision claim follows from the
IBP generation work.

Full four-loop production admission, the entire numerator/non-unit-mass native
acceptance inventory, complete or explicitly scoped certification, and a
representative FMFT-versus-native speed target remain unfinished. Finite-target
measurements must not be extrapolated to that whole inventory.

## Evidence and resumption

- [Four-loop generation benchmark](../four_loop_generation_comparison.md):
  completed solver timings, output counts, worker determinism and limitations.
- [Generation versus certification](../generation_and_certification.md): public
  split workflow and the authority boundaries of candidate loading/application.
- Local evidence is under ignored `TMP/rustred-four-loop-cpp-matched.VRhy3D/`
  and `TMP/vakint-candidate-4l.DRxEa4/`. These include frozen binaries, logs,
  scripts, hashes and full output files. Reference code and local notes remain
  untracked and are not part of the RustRed publication.

After resumption, first read the terminal measurements and publication status
below, then continue the outstanding C++ measurements and four-loop
bounded certification plus full Vakint acceptance. Do not rerun successful
expensive C++ work merely because its time was long. Do not replace failed or
censored observations by successful timings from another workload.

## Final verification and publication

RustRed source `d686d0987afd91dee057b6a0c35d89a72840a6c8` is committed and pushed
to `origin/main`. The focused gate passes 77 core and 11 candidate-bundle tests.
The broad gate passes 2,060 core tests (31 ignored) and 82 application tests,
with no failures. Formatting passes; Clippy completes with warnings.

GammaLoop now pins that pushed RustRed revision instead of an absolute local
path, and Cargo regenerated its lock. The checkout's own `nix develop` and a
focused experimental-feature `cargo check --locked` including tests pass.
Formatting and matching-toolchain Clippy also pass, with warnings.
Its clean local commit is `cd5f21e97f583ff19b3c3f5b888e68220654c5ea` on
`vakint_rustred`. Mandatory `ci-checks-and-upload` fails because the recipe is
absent; the configured cache credential path was also unreadable. No mandatory
CI success, cache upload or GammaLoop push is claimed without that gate.

All eight reconstruction measurements finished. Native H/FG/BMW cross-solver
canaries pass exact comparisons for twelve concrete targets after basis
transport. C++ BMW6 was checkpoint-censored after 3,005.99 seconds wall time;
uncapped X was not started. No owned benchmark job remains running. The
scientific goal is incomplete; work stops here for the user's quota pause.
