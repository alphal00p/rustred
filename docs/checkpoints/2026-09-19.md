# Four-loop saved-program checkpoint — 2026-09-19

## Scope and current delivery gate

Work resumed after the previous quota checkpoint. The immediate gate remains
all four-loop numerical tests through Vakint's public `EvaluationMethod::RustRed`,
including the 15 existing reference entrypoints and 16 expanded-propagator
numerator/pinch pairs. Five-loop work has **not** begun. The broader goal is not
complete, and this asset checkpoint is not an all-numerical-tests pass.

The completed slice removes the need to regenerate four-loop rules during
ordinary evaluation: four catalog-compatible candidate programs have now been
generated, saved, compressed and independently cold-loaded. The public Vakint
adapter and corrected FMFT oracle are undergoing the combined numerical gate.

## Completed assets and provenance

GammaLoop's `crates/vakint/data/rustred/four_loop/` now contains H, FG, BMW and X
inputs, ordered momentum descriptors, `.candidates.toml.gz` programs and `.rrcat`
terminal catalogs. Family names are omitted deliberately so the reconstructed
family fingerprint matches the existing `symbolica_integral` catalogs. Physical
propagator order, auxiliary coordinates and mass normalization are unchanged.
The first nine indices are physical for H/X; the first eight for FG/BMW.

Generation used RustRed's existing ordinary-source sparse-exact solver, default
ordering, six workers and CPU affinity 80–85. There were no imported FORM rules
or new algebraic/solver algorithms. Symbolica was the vendored 3.0 revision
`953e26e2754e9a4b918404fbdea590725d8e863d`, with the supplied license in the process
environment only. These are completed one-off preparation runs on a shared
host, **not** a new controlled RustRed/SpIRed performance comparison.

| Family | Solved sectors | Rules | Declared finite terminals | Raw bytes | Deterministic gzip bytes |
| --- | ---: | ---: | ---: | ---: | ---: |
| H | 314 | 21,360 | 386 | 240,831,356 | 6,757,903 |
| FG | 124 | 9,272 | 145 | 103,823,374 | 2,849,864 |
| BMW | 134 | 9,024 | 179 | 189,143,178 | 5,296,341 |
| X | 328 | 19,980 | 445 | 537,049,880 | 15,042,937 |
| Total | 900 | 59,636 | 1,155 | 1,070,847,788 | 29,947,045 |

The total terminal count sums family-local keys; it does not count independent
physical masters. The catalogs map these finite keys to FMFT's existing PR basis.
They are not new 20,000-digit numerical master evaluations.

All programs retain status `uncertified-candidates`. Neither successful loading
nor numerical agreement at supplied targets establishes unrestricted family
closure, source replay or minimality. No `ClosedArtifact` is claimed here.

## Timing boundaries and resource evidence

All times below are seconds. Compilation and dependency preparation are
excluded. `solve` is the prepared-source sector execution interval;
`encoding` includes candidate-record construction, validation and TOML encoding;
`API total` includes family/source preparation, solve and encoding, but excludes
the helper's final file writes and gzip compression. `cold load` measures the
fresh-process native decoder/rebuilder plus full catalog-key comparison after
the uncompressed file has been read. It excludes gzip decompression and is not
the hot scalar-reduction time.

| Family | Preparation | Solve | Encoding | API total | Cold load + catalog check |
| --- | ---: | ---: | ---: | ---: | ---: |
| H | 0.050409 | 25.424546 | 7.954634 | 33.429590 | 17.269794 |
| FG | 0.095641 | 24.691733 | 3.185791 | 27.973166 | 7.220907 |
| BMW | 0.058576 | 56.243367 | 6.125770 | 62.427715 | 12.627085 |
| X | 0.064172 | 99.952782 | 18.389581 | 118.406536 | 39.266891 |

GNU `time -v` separately measured the complete generator/helper and verifier
processes. CPU time is user plus system time. Peak RSS is reported directly in
KiB, not inferred from file size or summed across families.

| Family | Generation process wall | Generation CPU | Generation peak RSS (KiB) | Verification process wall | Verification CPU | Verification peak RSS (KiB) |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| H | 33.96 | 134.63 | 1,731,804 | 18.18 | 18.02 | 6,985,744 |
| FG | 28.19 | 66.26 | 808,468 | 7.62 | 7.55 | 3,256,352 |
| BMW | 62.83 | 143.35 | 1,322,184 | 13.24 | 13.14 | 4,988,968 |
| X | 119.65 | 329.51 | 3,567,228 | 41.22 | 40.84 | 12,942,336 |

The large cold-load RSS is real: TOML parsing and exact-expression reconstruction
allocate in-memory structures beyond the wire payload. Load-once reuse avoids
repeating this work; these measurements do not yet establish acceptable
whole-application cache memory or scalar throughput. Such optimization is not
part of this immediate numerical acceptance checkpoint.

## X budget failure and bounded correction

The initial X solve reached output construction but failed with
`candidate aggregate collection-entry budget exceeded` under the default
one-million-entry policy. It exited unsuccessfully after **121.62 s** wall,
364.76 s CPU and 1,453,172 KiB peak RSS; no usable bundle was saved. That attempt
is not included in the successful-run timing table and is not a completed timing.

The successful retry used explicit caller-owned bounds of 1 GiB serialized
bytes, 512 MiB aggregate coefficient text and eight million aggregate collection
entries. X alone contains 1,263,962 table-array records: 640,274 RHS terms,
602,935 source records, 19,980 rules, 445 residuals and 328 sectors, before
additional exceptional-case arrays. Its raw payload also exceeds the ordinary
256 MiB artifact limit. Thus both larger allowances address measured sizes.

The candidate codec now has a separate symmetric 1 GiB ceiling for explicitly
enlarged policies. Defaults remain 256 MiB, and certified-artifact limits and
schemas are unchanged. Existing checked accumulation of coefficient bytes and
collection entries remains in force. Wire limits do not promise an RSS limit.
CLI/Python default-budget surfaces have not acquired a larger-budget switch.

## Verification actually completed

- Every bundle loaded in a fresh process through
  `rustred_app::load_candidate_bundle::<10>` without rule generation.
- All four reconstructed family fingerprints matched their catalogs exactly.
- The complete declared terminal-key sets matched, not merely their counts:
  H 386, FG 145, BMW 179 and X 445.
- Every deterministic `gzip -n -9` file decompressed byte-for-byte to its saved
  uncompressed bundle.
- The H and FG six-worker serialized sector payloads matched previous
  two-worker outputs byte-for-byte after excluding the intentionally changed
  source/name header. This is a cross-worker check for those two families, not
  a new serial/six-worker determinism matrix for all four.
- The release candidate-bundle unit suite passed **13/13** in 0.09 s; a direct
  rerun of the same compiled test binary passed 13/13 in 0.08 s. Coverage includes
  K1/K3 save/load/replay, saved ordering and nonpositive roots, cross-worker
  determinism, malformed/tampered data rejection, unsupported scoped-proof
  rejection, exact serialized-byte boundary checks and unchanged artifact
  defaults. No four-loop closure claim follows from these small-case tests.

The independent budget audit checked symmetric read/write limits, finite
caller opt-in, checked arithmetic and unchanged certified-artifact defaults.
It explicitly identified wire-size versus peak-RSS accounting as a distinction
to retain in documentation.

## Reproduction and revision boundary

RustRed **`13bc9474` is published** and contains the candidate-only ceiling,
library/CLI feature separation, offline helper and budget tests. GammaLoop's
current RustRed and RustRed-app pins both reference this revision. Its runtime
API already accepts the explicit eight-million-entry caller policy used by the
new Vakint loader.

However, the helper **at that commit** still uses the default one-million-entry
count. Successful X generation used the subsequent helper amendment included
with this report: `max_collection_entries: 8_000_000`. Reproduction of all four families must
use a RustRed revision containing that helper amendment, or the same
explicit caller setting through the existing Rust API. Do not instruct users
to regenerate X with the unmodified helper at `13bc9474`. This does not require
regenerating the other three bundles or changing the runtime schema.

After building the helper from that revision, run from the RustRed checkout with
the license already set in the environment; keep all temporary paths inside
the workspace:

```sh
cargo build --release --locked --offline --no-default-features \
  -p rustred-app --example candidate_bundle

target/release/examples/candidate_bundle generate \
  /path/to/gammaloop/crates/vakint/data/rustred/four_loop/x.toml \
  9 6 default TMP/new-x.candidates.toml TMP/new-x.report.toml

# Separate process; no source search is performed here.
target/release/examples/candidate_bundle verify 10 \
  TMP/new-x.candidates.toml \
  /path/to/gammaloop/crates/vakint/data/rustred/four_loop/x.rrcat

gzip -n -9 -c TMP/new-x.candidates.toml > TMP/new-x.candidates.toml.gz
```

Use nonpositive coordinate list `9` for H/X and `8,9` for FG/BMW; output paths
must be new. The measured generation commands additionally used
`taskset -c 80-85` and `/run/current-system/sw/bin/time -v`.

The focused regression command was:

```sh
CARGO_PROFILE_RELEASE_LTO=off cargo test --release --locked --offline \
  -p rustred-app --no-default-features --lib \
  application::candidate_bundle::tests -- --test-threads=1
```

Local, ignored evidence is in `TMP/vakint-four-loop-assets-20260919/`:
`*.report.toml`, `*.generation.time`, `*.cold_load.{log,time}`,
`candidate-bundle-tests.log`, and the preserved helper executables/raw bundles.
For X, `x.generation.*` records the failed attempt and `x.wide-generation.*`
records the successful retry. No license is embedded in shipped data or docs.

## Corrected oracle and completed focused checks

The new cancellation tests exposed a real sign error in the existing FMFT
adapter: the FG basis needs `k4 = -p6`, not `+p6`. The correction preserves
absolute denominator IDs and uses signed IDs in numerator scalar products.
The focused FG/D7 versus P7 numerical regression now passes (0.71 s), while
its pre-correction failure remains recorded. A pure-Symbolica regression
checks all 19 registered four-loop classes and all 123 physical edges.

The corrected FMFT oracle re-evaluated all 1,155 declared terminals in
199.29 s: H 386 unchanged, FG 145 with four corrected numerator projections,
BMW 179 unchanged, X 445 unchanged. The shipped catalogs are byte-identical
to the fresh outputs. A subsequent FG check without the refresh/output option
passes with zero differences (23.62 s). No IBP program was regenerated for
these catalog corrections.

Focused release tests also pass: one signed-routing test, seven
default/support/mass regressions, and three public four-loop loading,
descriptor-binding and unexpanded-master-mode tests. The latter take 99.14 s
and peak at 12.67 GiB; their boundary includes the raw FG test plus four
sequential cold loads, not a single initialization or solver benchmark.

Evidence: `TMP/fg-diagnostic-{before,after}.log`,
`TMP/four-loop-catalog-audit.{log,time}`, `TMP/fg-catalog-recheck.log`,
`TMP/fmft-routing-regression.{log,time}`,
`TMP/four-loop-support-default-regressions.{log,time}`, and
`TMP/four-loop-public-unit-tests.{log,time}`.

## Incomplete gates at this checkpoint

The remaining delivery gates are the public-backend 15-reference inventory,
all 16 expanded-numerator/pinch pairs in both RustRed and FMFT, invalid-FORM
RustRed operation, relevant lower-loop/default regressions, final documentation
and coherent commits/pushes in both repositories. Only once those gates pass
may five-loop performance studies begin. The old instruction to stop at that
point was superseded by the user's September 19 resumption. Five-loop
studies and new performance exploration have not started in this resumed slice.
